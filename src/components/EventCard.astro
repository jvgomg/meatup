---
import LinkButton from './LinkButton.astro';

interface Venue {
  name: string;
  address?: string;
  googleMapsLink?: string;
}

interface Props {
  eventNumber: number;
  date: Date;
  primaryVenue: Venue;
  secondaryVenues?: Venue[];
  attendees: string[];
}

const { eventNumber, date, primaryVenue, secondaryVenues } = Astro.props;
---

<li class="event">
  <div class="event-number">
    {eventNumber.toString().padStart(3, '0')}
  </div>
  <div class="details">
    <h2>{primaryVenue.name}</h2>
    <time datetime={date.toISOString()}>
      {date.toLocaleDateString('en-GB', {
        year: 'numeric',
        month: 'long',
        day: 'numeric'
      })}
    </time>
    {primaryVenue.googleMapsLink && (
      <LinkButton href={primaryVenue.googleMapsLink} target="_blank" rel="noopener nofollow">Open in Maps</LinkButton>
    )}
    {secondaryVenues && secondaryVenues.length > 0 && (
      <ul class="secondary-venues">
        {secondaryVenues.map((venue) => (
          <li>
            <span>{venue.name}</span>
            {venue.googleMapsLink && (
              <a href={venue.googleMapsLink} target="_blank" rel="noopener nofollow">Open in Maps</a>
            )}
          </li>
        ))}
      </ul>
    )}
  </div>
</li>

<style>
  .event {
    border: 2px solid var(--stark-white);
    padding: 2rem;
    display: flex;
    align-items: center;
    gap: 2rem;
  }

  .event-number {
    font-family: var(--font-display);
    font-size: 4rem;
    color: var(--mustard-yellow);
  }

  .details h2 {
    font-size: var(--text-section);
  }

  .details time {
    opacity: 0.7;
    display: block;
    margin: 0.5rem 0;
  }

  .secondary-venues {
    list-style: none;
    padding: 0;
    margin: 1rem 0 0;
    opacity: 0.7;
    font-size: 0.9rem;
  }

  .secondary-venues li {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin: 0.25rem 0;
  }

  .secondary-venues a {
    text-decoration: underline;
  }
</style>
